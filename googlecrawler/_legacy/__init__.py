"""Legacy single-threaded scraping modules (kept for reference only).

The active pipeline uses the multi-threaded variants in the parent package
(``extracturls_threading.py``, ``crawler_threading.py``).
"""
